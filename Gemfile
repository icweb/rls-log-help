source "https://rubygems.org"

# GitHub Pages dependency bundle — pinned versions of jekyll, kramdown, rouge,
# and the small set of plugins GH Pages allows. This lets `bundle exec jekyll
# serve` produce output identical to what GitHub builds.
gem "github-pages", group: :jekyll_plugins
